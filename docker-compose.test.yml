# Docker Compose configuration for running tests
# Usage: docker-compose -f docker-compose.test.yml up -d
# Or: docker-compose -f docker-compose.test.yml run --rm test composer test

services:
  test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: login-throttle-bundle-test
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - login-throttle-bundle-test-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  login-throttle-bundle-test-network:
    driver: bridge

